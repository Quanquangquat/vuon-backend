# Railway deployment configuration for VUON Backend (Spring Boot)
[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/vuon-backend-1.0.0.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
